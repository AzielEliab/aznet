# AZNet — iPhone & Android

Pair, unlock, stamp, and verify hashes on device.
Hashes only. AZNet + AZBrowser both required.

Offline. No analytics. Black / white / gold.

Application id: `com.azieeliab.aznet`

## Open in Android Studio / Xcode

The `android/` and `ios/` folders here are skeleton READMEs because
this tree was written without the Flutter SDK on PATH.

```bash
cd mobile
flutter create --org com.azieeliab --project-name aznet .
flutter pub get
flutter run
```

Then open `android/` in Android Studio, or `ios/Runner.xcworkspace` in
Xcode.

## Honest scope

Silent verification side-net. Not an alt internet. Not a host.
Not a payload store. Author Aziel Eliab.

## Desktop package (counted download)

This phone app does not replace the desktop package.

# → https://aznet-download-tracker.vibelock.workers.dev/ ←

GitHub: https://github.com/AzielEliab/aznet

**Forks are welcome and always allowed.**
